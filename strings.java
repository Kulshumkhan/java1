import java.util.Scanner;

public class strings {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        String sentence="my name is zakiya.";
       // String name =sentence.substring(0,sentence.length());
        String name =sentence.substring(0,7);
        System.out.println(name);
      /*  String firstname="zakiya";
        String lastname="khatoon";
      String fullname=lastname+firstname;
        System.out.println(fullname);*/
       // String firstname=sc.nextLine();
      //  String lastname=sc.nextLine();
       // String fullname =firstname+" "+lastname;
       // System.out.println(fullname.length());
       // for (int i=0;i<fullname.length();i++){
         //   System.out.println(fullname.charAt(i));
           // if (firstname.compareTo(lastname)==0){
           //     System.out.println("both are equall");
            //}
            //else {
              //  System.out.println("not equall");
            //}
        //strings are immutable.
        }
    }

